{
  "private": true,
  
  "workspaces": [
    "client",
    "server"
  ],
    "scripts": {
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish",
    "client:add": "npm install --workspace client",
    "server:add": "npm install --workspace server"
  },
  "dependencies": {
    "@changesets/cli": "^2.31.1"
  }
}
